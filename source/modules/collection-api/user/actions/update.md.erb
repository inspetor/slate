### Account Updates
```sh
curl -X PUT https://collection-prod.inspcdn.net/user \
    -H "Authorization: Bearer $API_KEY" \
    -d '{
        "id":            "12345",
        "email":         "my_new_email@inspetor.com",
        "address":       {
            "street_name":   "Rua Novarua",
            "street_number": "253",
            "zip_code":      "05432-010",
            "city":          "São Paulo",
            "state":         "SP",
            "country":       "BR"
        }
    }'
```

Send information to Inspetor any time an user's information (email, address, name, password, etc...) is altered.