### Event Deletion

```sh
curl -XDELETE https://collection-prod.inspcdn.net/event \
    -H 'Authorization: Bearer <your API key>'   
    -d '{
        "id": "12345"
    }'
```

Send information to Inspetor every time an Event listing is deleted from your platform
