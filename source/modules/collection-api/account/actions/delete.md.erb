### Account Deletion
```sh
curl -X DELETE https://collection-prod.inspcdn.net/account \
    -H "Authorization: Bearer $API_KEY" \
    -d '{
        "id": "12345"
    }'
```

Send information to Inspetor any time an account is deleted.