### Account Deletion
```sh
curl -XDELETE https://collection-prod.inspcdn.net/account \
    -H 'Authorization: Bearer <your API key>'
    -d '{
        "id": "12345"
    }'
```

Send information to Inspetor any time an account is deleted.