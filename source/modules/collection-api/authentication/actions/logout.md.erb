### Account Logout

```php
<?php

use Inspetor;

$inspetor = $this->getConfiguredInspetor();

$inspetor->trackLogout($auth);

?>
```
```python 
import inspetor

inspetor = self.getConfiguredInspetor()

inspetor.track_logout(auth)

```

Send information to Inspetor every time an account (*e.g. user*) logs out.

Argument | Type | Description
-------- | ---- | -----------
auth  | Inspetor/Model/[InspetorAuth](#inspetorauth) | The Auth object that describes the logout