### Transfer Updates
```shell
curl -XPUT https://collection-prod.inspcdn.net/transfer \
    -d '{
        "id":     "43262",
        "status": "accepted"
    }'      

```


This function is used to send information to Inspetor every time an item (*e.g. ticket*) transfer's status is updated.
