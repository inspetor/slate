<h2 id='inspetorauth'>InspetorAuth</h2>
<p>Login and logout event information in your platform</p>
<h3 id='properties'>Properties</h3><pre class="highlight php tab-php"><code><span class="cp">&lt;?php</span>

<span class="c1">// Calling an instance of Model
</span><span class="nv">$inspetor_auth</span> <span class="o">=</span> <span class="nv">$inspetor</span><span class="o">-&gt;</span><span class="na">getInspetorAuth</span><span class="p">();</span> 

<span class="c1">// Filling model with company data
</span><span class="nv">$inspetor_auth</span><span class="o">-&gt;</span><span class="na">setAccountEmail</span><span class="p">(</span><span class="s2">"login@email.com"</span><span class="p">);</span> <span class="c1">//getAccountEmail()
</span><span class="nv">$inspetor_auth</span><span class="o">-&gt;</span><span class="na">setTimestamp</span><span class="p">(</span><span class="nb">time</span><span class="p">());</span> <span class="c1">//getTimestamp()
</span><span class="nv">$inspetor_auth</span><span class="o">-&gt;</span><span class="na">setSucceeded</span><span class="p">(</span><span class="kc">true</span><span class="p">);</span> <span class="c1">//getSucceeded()
</span>

<span class="nv">$inspetor</span><span class="o">-&gt;</span><span class="na">trackAccountLogin</span><span class="p">(</span><span class="nv">$inspetor_auth</span><span class="p">);</span>
<span class="nv">$inspetor</span><span class="o">-&gt;</span><span class="na">trackAccountLogout</span><span class="p">(</span><span class="nv">$inspetor_auth</span><span class="p">);</span>

<span class="cp">?&gt;</span>
</code></pre><pre class="highlight python tab-python"><code><span class="c"># Calling an instance of Model</span>
<span class="n">inspetor_auth</span> <span class="o">=</span> <span class="bp">self</span><span class="o">.</span><span class="n">inspetor</span><span class="o">.</span><span class="n">get_inspetor_auth</span><span class="p">()</span>

<span class="c"># Filling model with auth data</span>
<span class="n">inspetor_auth</span><span class="o">.</span><span class="n">account_email</span> <span class="o">=</span> <span class="s">"test@email.com"</span>
<span class="n">inspetor_auth</span><span class="o">.</span><span class="n">succeeded</span> <span class="o">=</span> <span class="n">datetime</span><span class="o">.</span><span class="n">timestamp</span><span class="p">(</span><span class="n">datetime</span><span class="o">.</span><span class="n">now</span><span class="p">())</span>
<span class="n">inspetor_auth</span><span class="o">.</span><span class="n">timestamp</span> <span class="o">=</span> <span class="bp">True</span> <span class="o">//</span> <span class="bp">True</span> <span class="n">when</span> <span class="n">login</span> <span class="n">works</span>


<span class="n">inspetor</span><span class="o">.</span><span class="n">track_login</span><span class="p">(</span><span class="n">inspetor_auth</span><span class="p">)</span>
<span class="n">inspetor</span><span class="o">.</span><span class="n">track_logout</span><span class="p">(</span><span class="n">inspetor_auth</span><span class="p">)</span>
</code></pre>
<p>All properties can be accessed or defined via <code>get</code> and <code>set</code> accessor methods</p>

<table><thead>
<tr>
<th>Property</th>
<th>Required For Login</th>
<th>Required For Logout</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead><tbody>
<tr>
<td><strong>account_email</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>String</td>
<td>The email of the user that is attempting to login or logout</td>
</tr>
<tr>
<td><strong>timestamp</strong></td>
<td>Yes</td>
<td>Yes</td>
<td>Integer</td>
<td>The unix format of the time and date that the authentication happend</td>
</tr>
<tr>
<td><strong>succeeded</strong></td>
<td>Yes</td>
<td>No</td>
<td>Boolean</td>
<td>Indicating if the attempt to login our logout was succeeded or not.</td>
</tr>
</tbody></table>
